# 极速进程管家 —— 手工指定本机 CPU 功耗上限（可选）
#
# 用法：把本文件复制一份、改名为 power_limit.txt（去掉 .example），
#       程序启动时会优先读它，忽略内置型号表。
#
# 两个数字 = 基础功耗(PL1) 最大睿频功耗(PL2)，单位 W
# 只写一个数字 = 最大睿频功耗(PL2)
#
# 例（Intel Core Ultra X7 358H）：
# 25 80
